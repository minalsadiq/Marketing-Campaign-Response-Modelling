# Extra dependencies needed ONLY to re-run the notebooks (profiling, EDA,
# baseline training, tuning, evaluation/interpretability plots) -- NOT
# needed to run the deployed app. Install on top of requirements.txt:
#
#   pip install -r requirements.txt -r requirements-notebooks.txt
#
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0
xgboost==2.0.3
lightgbm==4.3.0
shap==0.45.1
optuna==3.6.1
